//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

final class {{ module_info.name }}ModuleConfigurator: BaseConfigurator {

    // MARK: - Internal methods

    func configure(output: {{ module_info.name }}ModuleOutput? = nil) -> (viewController: {{ module_info.name }}ViewController, input: {{ module_info.name }}ModuleInput) {
        guard let view = UIStoryboard(name: String(describing: {{ module_info.name }}ViewController.self),
                                      bundle: Bundle.main).instantiateInitialViewController() as? {{ module_info.name }}ViewController else {
            fatalError("Can't load {{ module_info.name }}ViewController from storyboard, check that controller is initial view controller")
        }
        let presenter = {{ module_info.name }}Presenter()
        let router = {{ module_info.name }}Router(services: services)

        presenter.view = view
        presenter.router = router
        presenter.output = output
        router.view = view
        view.output = presenter

        return (view, presenter)
    }

}
